{% extends 'base.html.twig' %}
{% block title %}Mon Profil{% endblock %}
{% block body %}
<div id="formulaire">
  <img src="/img/chair.jpeg">
</div>
<div class="container">
  <div class="row">
    <div class="col-md-3 navProfil">
      <ul class="nav nav-tabs">
        <li><a id="link_dashboard" data-toggle="tab" href="#dashboard" class="list-group-item list-group-item-action active">Tableau de bord</a></li>
        <li><a id="link_reservations" data-toggle="tab" href="#reservations" class="list-group-item list-group-item-action">Mes réservations</a></li>
        <li><a id="link_messages" data-toggle="tab" href="#messages" class="list-group-item list-group-item-action">Messages</a></li>
        <li><a id="link_informations" data-toggle="tab" href="#informations" class="list-group-item list-group-item-action">Mon Profil</a></li>
      </ul>
    </div>
    <div class="col-md-8 offset-md-1">
      <div class="card noborder">
        <div class="cardProfil">
          <div class="row">
            <div class="col-md-12">
              <div class="tab-content">
                <div class="tab-pane" id="dashboard">
                 <div class="row">
                  <div class="col-md-12">
                    <h4>Tableau de bord</h4>
                    <hr>
                  </div>
                </div>
              </div>
              {# Mes réservations #}
              <div class="tab-pane" id="reservations">
                <div class="row">
                  <div class="col-md-12">
                    <h4>Mes réservations</h4>
                    <hr>
                    <div id="accordion">
                      <div class="card">
                        <div class="card-header" id="headingOne">
                          <h5 class="mb-0">
                            <button class="btn btn-link" data-toggle="collapse" data-target="#collapseOne" aria-expanded="true" aria-controls="collapseOne">
                              Voyages en cours ou à venir
                            </button>
                          </h5>
                        </div>
                        <div id="collapseOne" class="collapse show" aria-labelledby="headingOne" data-parent="#accordion">
                          {% if newResas is not empty %}
                            {% for r in newResas %}
                              <div class="card-body">
                                <div class="boxReservation">
                                  <img class="img" src="/uploads/logements/{{ r.getLogement().getId() ~ '/' ~ r.getLogement().getPhotos[0].getPhoto() }}">
                                  <h5 class="card-title">{{ r.getLogement().getNom() }} du {{ r.getDateDebut()|date('d/m/Y') }} au {{ r.getDateFin()|date('d/m/Y') }}</h5>
                                  <p class="card-text">{{ r.getLogement().getDescription() }}</p>
                                  <p class="card-text"><small class="text-muted">Réservé le {{ r.getDateCreation()|date('d/m/Y') }}</small></p>
                                </div>
                              </div>
                            {% endfor %}
                          {% else %}
                            <div class="card-body">Aucun voyage en cours et/ou à venir.</div>
                          {% endif %}
                        </div>
                      </div>
                      <div class="card">
                        <div class="card-header" id="headingTwo">
                          <h5 class="mb-0">
                            <button class="btn btn-link collapsed" data-toggle="collapse" data-target="#collapseTwo" aria-expanded="false" aria-controls="collapseTwo">
                              Historiques de vos séjours
                            </button>
                          </h5>
                        </div>
                        <div id="collapseTwo" class="collapse" aria-labelledby="headingTwo" data-parent="#accordion">
                          {% if newResas is not empty %}
                          {% for r in oldResas %}
                            <div class="card-body">
                              <div class="boxReservation">
                                <img class="img" src="/uploads/logements/{{ r.getLogement().getId() ~ '/' ~ r.getLogement().getPhotos[0].getPhoto() }}">
                                <h5 class="card-title">{{ r.getLogement().getNom() }} du {{ r.getDateDebut()|date('d/m/Y') }} au {{ r.getDateFin()|date('d/m/Y') }}</h5>
                                <p class="card-text">{{ r.getLogement().getDescription() }}</p>
                                <p class="card-text"><small class="text-muted">Réservé le {{ r.getDateCreation()|date('d/m/Y') }}</small></p>
                              </div>
                            </div>
                          {% endfor %}
                          {% else %}
                            <div class="card-body">Aucun voyage dans l'historique.</div>                            
                          {% endif %}
                        </div>
                      </div>
                    </div>
                  </div>
                </div>
            </div>
            {# Informations #}
            <div class="tab-pane" id="informations">
             <div class="row">
              <div class="col-md-12">
                <h4>Mon profil</h4>
                <hr>
                {% for flash_message in app.session.flashBag.get('error') %}
                    <div class="alert alert-danger">
                        {{ flash_message }}
                    </div>
                {% endfor %}
                {% for flash_message in app.session.flashBag.get('success') %}
                    <div class="alert alert-success">
                        {{ flash_message }}
                    </div>
                {% endfor %}
                {{ form_start(form) }}
                  <div class="form-group row">
                    {{ form_label(form.nom)}}
                    <div class="col-8">
                      {{ form_widget(form.nom)}}
                    </div>
                  </div>
                  <div class="form-group row">
                    {{ form_label(form.prenom)}}
                    <div class="col-8">
                      {{ form_widget(form.prenom)}}
                    </div>
                  </div>
                  <div class="form-group row">
                    {{ form_label(form.email)}}
                    <div class="col-8">
                      {{ form_widget(form.email)}}
                    </div>
                  </div>
                  <div class="form-group row">
                    {{ form_label(form.telephone)}}
                    <div class="col-8">
                      {{ form_widget(form.telephone)}}
                    </div>
                  </div>
                  <div class="form-group row">
                    {{ form_label(form.adresse)}}
                    <div class="col-8">
                      {{ form_widget(form.adresse)}}
                    </div>
                  </div>
                  <div class="form-group now">
                    <div class="col-8">
                      {{ form_widget(form.newsletter)}}&nbsp;&nbsp;J'accepte de recevoir les newsletter d'Atypik'House.
                    </div>
                  </div>
                  <div class="form-group row">
                      <button name="submit" type="submit" class="btn btn-success">Mettre à jour mon profil</button>
                  </div>
                {{ form_end(form) }}
              </div>
            </div>
          </div>
          {# Messages #}
          <div class="tab-pane" id="messages">
           <div class="row">
            <div class="col-md-12">
              <h4>Messages</h4>
              <hr>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
</div>
</div>
</div>
</div>
{% endblock %}

{% block javascripts %}
  {{ parent() }}
  <script type="text/javascript" src="/js/profil.js"></script>  
{% endblock %}
