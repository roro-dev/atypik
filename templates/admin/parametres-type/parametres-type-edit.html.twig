{% extends 'base.html.twig' %}

{% block title %}Edit ParametresType{% endblock %}

{% block body %}
    <h1>Edit ParametresType</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}


    <a href="{{ path('parametres_type_index') }}">back to list</a>

    {{ include('parametres_type/_delete_form.html.twig') }}
{% endblock %}
