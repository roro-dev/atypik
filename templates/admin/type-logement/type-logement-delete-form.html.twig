<form method="post" action="{{ path('type_logement_delete', {'id': type_logement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_logement.id) }}">
    <button class="btn">Delete</button>
</form>
