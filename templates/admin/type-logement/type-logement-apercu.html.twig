{% extends 'base.html.twig' %}

{% block title %}TypeLogement{% endblock %}

{% block body %}
    <h1>TypeLogement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_logement.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ type_logement.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_logement_index') }}">back to list</a>

    <a href="{{ path('type_logement_edit', {'id': type_logement.id}) }}">edit</a>

    {{ include('type_logement/_delete_form.html.twig') }}
{% endblock %}
