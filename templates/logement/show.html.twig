{% extends 'base.html.twig' %}

{% block title %}Logement{% endblock %}

{% block body %}
    <h1>Logement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ logement.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ logement.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ logement.description }}</td>
            </tr>
            <tr>
                <th>NbVoyageur</th>
                <td>{{ logement.nbVoyageur }}</td>
            </tr>
            <tr>
                <th>NbLits</th>
                <td>{{ logement.nbLits }}</td>
            </tr>
            <tr>
                <th>NbSalledeBain</th>
                <td>{{ logement.nbSalledeBain }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ logement.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('logement_index') }}">back to list</a>

    <a href="{{ path('logement_edit', {'id': logement.id}) }}">edit</a>

    {{ include('logement/_delete_form.html.twig') }}
{% endblock %}
